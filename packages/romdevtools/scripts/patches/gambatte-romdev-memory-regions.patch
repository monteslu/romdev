diff --git a/libgambatte/include/gambatte.h b/libgambatte/include/gambatte.h
index 7e5c43b..4530bc7 100644
--- a/libgambatte/include/gambatte.h
+++ b/libgambatte/include/gambatte.h
@@ -147,6 +147,16 @@ public:
    void *rombank1_ptr() const;
    void *zeropage_ptr() const;
    void *oamram_ptr() const;
+   /* rom-dev-mcp extension: extra state accessors for inspector tools. */
+   void *iohram_ptr() const;
+   void *bgpdata_ptr() const;
+   void *objpdata_ptr() const;
+   void  cpu_snapshot(unsigned char *out) const;
+   bool  isCgbMode() const;
+   /* rom-dev-mcp RE round 2: SM83 register read/write by romdev reg-id
+    * (0=A 1=F 2=B 3=C 4=D 5=E 6=H 7=L 16=PC 18=SP). */
+   int      romdev_setReg(int regId, unsigned value);
+   unsigned romdev_getReg(int regId) const;
 #endif
 
 private:
diff --git a/libgambatte/libretro/libretro.cpp b/libgambatte/libretro/libretro.cpp
index 15b042c..4947f08 100644
--- a/libgambatte/libretro/libretro.cpp
+++ b/libgambatte/libretro/libretro.cpp
@@ -2874,6 +2874,40 @@ void retro_unload_game()
 
 unsigned retro_get_region() { return RETRO_REGION_NTSC; }
 
+/* romdev (0.80.0): the watchpoint/readwatch/range/coverage/pcbreak/watchdog exports
+ * now come from the shared romdev_debug.c (built into the core). Only the GB-specific
+ * romdev_setReg/getReg + the memory-region accessors stay here. */
+extern "C" {
+#include "romdev_debug.h"
+
+/* romdev register read/write — the inverse of getCPUState (callSubroutine /
+ * setRegister). SM83 reg-id convention (STABLE across cores): 0=A 1=F 2=B 3=C
+ * 4=D 5=E 6=H 7=L 16=PC 18=SP. Returns 0 on success, -1 on an unknown id.
+ * The setreg of PC writes pc_ (the field CPU::process loads into its `pc` local),
+ * so it takes effect on the next run — see CPU::romdev_setReg for the gotcha. */
+int romdev_setreg(int regId, unsigned value) {
+   return gb.romdev_setReg(regId, value);
+}
+
+unsigned romdev_getreg(int regId) {
+   return gb.romdev_getReg(regId);
+}
+
+}
+
+/* rom-dev-mcp extension: custom memory region IDs above libretro standard.
+ * Lets the host inspect GB/GBC PPU state for inspectSprites / inspectPalette
+ * / getCPUState. Mirrors fceumm/snes9x/gpgx patches. */
+#define ROMDEV_MEMORY_GB_VRAM     0x140  /* 8KB DMG / 16KB GBC */
+#define ROMDEV_MEMORY_GB_OAM      0x141  /* 160B sprite OAM */
+#define ROMDEV_MEMORY_GB_IO       0x142  /* 128B IO regs $FF00-$FF7F */
+#define ROMDEV_MEMORY_GB_HRAM     0x143  /* 128B high RAM $FF80-$FFFE + IE */
+#define ROMDEV_MEMORY_GB_BGPDATA  0x144  /* 64B GBC BG palette RAM */
+#define ROMDEV_MEMORY_GB_OBJPDATA 0x145  /* 64B GBC sprite palette RAM */
+#define ROMDEV_MEMORY_GB_CPU_REGS 0x146  /* 18B SM83 CPU snapshot */
+
+static unsigned char romdev_gb_cpu_buf[18];
+
 void *retro_get_memory_data(unsigned id)
 {
    if (rom_loaded) switch (id)
@@ -2883,11 +2917,20 @@ void *retro_get_memory_data(unsigned id)
       case RETRO_MEMORY_RTC:
          return gb.rtcdata_ptr();
       case RETRO_MEMORY_SYSTEM_RAM:
-         /* Really ugly hack here, relies upon 
+         /* Really ugly hack here, relies upon
           * libgambatte/src/memory/memptrs.cpp MemPtrs::reset not
-          * realizing that that memchunk hack is ugly, or 
+          * realizing that that memchunk hack is ugly, or
           * otherwise getting rearranged. */
          return gb.rambank0_ptr();
+      case ROMDEV_MEMORY_GB_VRAM:     return gb.vram_ptr();
+      case ROMDEV_MEMORY_GB_OAM:      return gb.oamram_ptr();
+      case ROMDEV_MEMORY_GB_IO:       return gb.iohram_ptr();
+      case ROMDEV_MEMORY_GB_HRAM:     return gb.zeropage_ptr();
+      case ROMDEV_MEMORY_GB_BGPDATA:  return gb.bgpdata_ptr();
+      case ROMDEV_MEMORY_GB_OBJPDATA: return gb.objpdata_ptr();
+      case ROMDEV_MEMORY_GB_CPU_REGS:
+         gb.cpu_snapshot(romdev_gb_cpu_buf);
+         return romdev_gb_cpu_buf;
    }
 
    return 0;
@@ -2902,14 +2945,21 @@ size_t retro_get_memory_size(unsigned id)
       case RETRO_MEMORY_RTC:
          return gb.rtcdata_size();
       case RETRO_MEMORY_SYSTEM_RAM:
-         /* This is rather hacky too... it relies upon 
+         /* This is rather hacky too... it relies upon
           * libgambatte/src/memory/cartridge.cpp not changing
-          * the call to memptrs.reset, but this is 
+          * the call to memptrs.reset, but this is
           * probably mostly safe.
           *
           * GBC will probably not get a
           * hardware upgrade anytime soon. */
          return (gb.isCgb() ? 8 : 2) * 0x1000ul;
+      case ROMDEV_MEMORY_GB_VRAM:     return gb.isCgbMode() ? 0x4000 : 0x2000;
+      case ROMDEV_MEMORY_GB_OAM:      return 0xA0;
+      case ROMDEV_MEMORY_GB_IO:       return 0x80;
+      case ROMDEV_MEMORY_GB_HRAM:     return 0x80;
+      case ROMDEV_MEMORY_GB_BGPDATA:  return 0x40;
+      case ROMDEV_MEMORY_GB_OBJPDATA: return 0x40;
+      case ROMDEV_MEMORY_GB_CPU_REGS: return sizeof(romdev_gb_cpu_buf);
    }
 
    return 0;
diff --git a/libgambatte/src/cpu.cpp b/libgambatte/src/cpu.cpp
index 0fb6199..7de286d 100644
--- a/libgambatte/src/cpu.cpp
+++ b/libgambatte/src/cpu.cpp
@@ -20,6 +20,23 @@
 #include "gambatte-memory.h"
 #include "savestate.h"
 
+/* romdev (0.80.0): all the watchpoint/readwatch/range/coverage/pcbreak/watchdog
+ * STATE + machinery now lives in the shared romdev_debug.c. cpu.cpp keeps only the
+ * GB-specific bits: the live-PC latch the hooks pass in, and the at-hit register
+ * snapshot (the sm83 register layout — genuinely per-core). The WRITE/READ macros
+ * below call romdev_on_write/on_read/on_dispatch from the shared lib. */
+extern "C" {
+#include "romdev_debug.h"
+/* romdev_gb_instr_pc — the address of the currently-executing instruction, latched
+ * at dispatch in CPU::process; the hooks stamp it as the writer/reader PC. */
+unsigned romdev_gb_instr_pc = 0xFFFFFFFF;
+/* at-hit REGISTER SNAPSHOT (sm83). The CPU state lives in LOCALS inside CPU::process;
+ * snapshot into romdev_snap_regs[] (shared) at the hit instant. Layout = the
+ * romdev_getReg regId order: [0]=A [1]=F [2]=B [3]=C [4]=D [5]=E [6]=H [7]=L
+ * [16]=PC of the executing instruction [18]=SP. (romdev_snap_regs / romdev_snap_kind
+ * are defined in romdev_debug.c.) */
+}
+
 namespace gambatte {
 
 CPU::CPU()
@@ -130,6 +147,57 @@ void CPU::loadState(SaveState const &state) {
 	skip_ = state.cpu.skip;
 }
 
+#ifdef __LIBRETRO__
+/* rom-dev-mcp RE round 2 — register read/write by romdev reg-id. SM83 convention
+ * (STABLE across cores): 0=A 1=F 2=B 3=C 4=D 5=E 6=H 7=L 16=PC 18=SP.
+ *
+ * The setreg-of-PC GOTCHA: CPU::process() copies pc_ into a LOCAL `pc` at the top
+ * of its outer loop and only writes it back to pc_ on exit. So to make a setreg of
+ * PC actually take on the NEXT process() call, we must write pc_ (the field
+ * process loads), NOT a stale local — which is what we do here. A (a_) is the same
+ * shape: process copies a_ into a local `a` at entry and writes it back on exit, so
+ * we write a_. sp/b/c/d/e/h/l are plain members read directly each instruction.
+ * The flags live split across hf1/hf2/zf/cf; we synthesize F with the file-local
+ * toF() (read) and decompose with zfFromF/hf2FromF/cfFromF (write), exactly like
+ * saveState/loadState — so a getReg after a setReg of F round-trips. */
+int CPU::romdev_setReg(int regId, unsigned value) {
+	switch (regId) {
+	case 0:  a_ = value & 0xFF; return 0;                 /* A  → a_ (process loads this at entry) */
+	case 1:                                               /* F  → split flag fields */
+		zf  =  zfFromF(value & 0xFF);
+		hf2 = hf2FromF(value & 0xFF);
+		hf1 = 0;                                          /* keep hf1 neutral; hf2 carries the H bit */
+		cf  =  cfFromF(value & 0xFF);
+		return 0;
+	case 2:  b = value & 0xFF; return 0;
+	case 3:  c = value & 0xFF; return 0;
+	case 4:  d = value & 0xFF; return 0;
+	case 5:  e = value & 0xFF; return 0;
+	case 6:  h = value & 0xFF; return 0;
+	case 7:  l = value & 0xFF; return 0;
+	case 16: pc_ = value & 0xFFFF; return 0;              /* PC → pc_ (process loads this each outer iter) */
+	case 18: sp = value & 0xFFFF; return 0;               /* SP → sp member */
+	default: return -1;
+	}
+}
+
+unsigned CPU::romdev_getReg(int regId) const {
+	switch (regId) {
+	case 0:  return a_;
+	case 1:  return toF(hf2, cf, zf) & 0xFF;
+	case 2:  return b;
+	case 3:  return c;
+	case 4:  return d;
+	case 5:  return e;
+	case 6:  return h;
+	case 7:  return l;
+	case 16: return pc_;
+	case 18: return sp;
+	default: return 0;
+	}
+}
+#endif
+
 // The main reasons for the use of macros is to more conveniently be able to tweak
 // which variables are local and which are not, combined with the fact that at the
 // time they were written GCC had a tendency to not be able to keep hot variables
@@ -139,12 +207,44 @@ void CPU::loadState(SaveState const &state) {
 #define de() ( d << 8 | e )
 #define hl() ( h << 8 | l )
 
-#define READ(dest, addr) do { (dest) = mem_.read(addr, cycleCounter); cycleCounter += 4; } while (0)
+/* Capture the at-hit register snapshot from CPU::process's LOCALS (a/b/c/…/sp
+ * are locals there; this macro only expands in that scope). PC reported is the
+ * EXECUTING instruction's first byte (romdev_gb_instr_pc, latched at dispatch),
+ * not the operand-advanced live pc. */
+#define ROMDEV_GB_SNAP(kind_) do { \
+		int snap_i_; \
+		for (snap_i_ = 0; snap_i_ < 19; snap_i_++) romdev_snap_regs[snap_i_] = 0; \
+		romdev_snap_regs[0]  = a; \
+		romdev_snap_regs[1]  = toF(hf2, cf, zf) & 0xFF; \
+		romdev_snap_regs[2]  = b;  romdev_snap_regs[3] = c; \
+		romdev_snap_regs[4]  = d;  romdev_snap_regs[5] = e; \
+		romdev_snap_regs[6]  = h;  romdev_snap_regs[7] = l; \
+		romdev_snap_regs[16] = romdev_gb_instr_pc; \
+		romdev_snap_regs[18] = sp; \
+		romdev_snap_kind = (kind_); \
+	} while (0)
+
+/* romdev READ watchpoint check — runs AFTER the data read so it captures the
+ * value actually returned. Mirror of ROMDEV_WP_CHECK; hooked into the data-read
+ * macros below. PC_READ (instruction/operand fetch) is deliberately NOT hooked. */
+/* romdev (0.80.0 shared lib): the read/write/range bookkeeping now lives in
+ * romdev_debug.c — these macros just feed it the gambatte-specific bits (the live
+ * sm83 PC + the active-bank ROM offset) and take the per-CPU register snapshot when
+ * the shared lib reports a hit. ROMDEV_GB_SNAP fills romdev_snap_regs[] (the GB
+ * register layout — genuinely per-core). See scripts/romdev-debug/romdev_debug.h. */
+#define ROMDEV_RD_CHECK(a, d) do { \
+		if (romdev_on_read((unsigned)(a), (unsigned char)(d), romdev_gb_instr_pc)) { ROMDEV_GB_SNAP(4); } \
+	} while (0)
+#define READ(dest, addr) do { (dest) = mem_.read(addr, cycleCounter); ROMDEV_RD_CHECK((addr), (dest)); cycleCounter += 4; } while (0)
 #define PC_READ(dest) do { (dest) = mem_.read(pc, cycleCounter); pc = (pc + 1) & 0xFFFF; cycleCounter += 4; } while (0)
-#define FF_READ(dest, addr) do { (dest) = mem_.ff_read(addr, cycleCounter); cycleCounter += 4; } while (0)
+#define FF_READ(dest, addr) do { (dest) = mem_.ff_read(addr, cycleCounter); ROMDEV_RD_CHECK(0xFF00 | ((addr) & 0xFF), (dest)); cycleCounter += 4; } while (0)
 
-#define WRITE(addr, data) do { mem_.write(addr, data, cycleCounter); cycleCounter += 4; } while (0)
-#define FF_WRITE(addr, data) do { mem_.ff_write(addr, data, cycleCounter); cycleCounter += 4; } while (0)
+#define ROMDEV_WP_CHECK(a, d) do { \
+		unsigned char romdev_oldv = romdev_wp_wants_old() ? (unsigned char)mem_.read((a), cycleCounter) : 0; \
+		if (romdev_on_write((unsigned)(a), romdev_oldv, (unsigned char)(d), romdev_gb_instr_pc, mem_.romdev_romOffset(romdev_gb_instr_pc))) { ROMDEV_GB_SNAP(3); } \
+	} while (0)
+#define WRITE(addr, data) do { ROMDEV_WP_CHECK((addr), (data)); mem_.write(addr, data, cycleCounter); cycleCounter += 4; } while (0)
+#define FF_WRITE(addr, data) do { ROMDEV_WP_CHECK(0xFF00 | ((addr) & 0xFF), (data)); mem_.ff_write(addr, data, cycleCounter); cycleCounter += 4; } while (0)
 
 #define PC_MOD(data) do { pc = data; cycleCounter += 4; } while (0)
 
@@ -499,6 +599,19 @@ void CPU::process(unsigned long const cycles) {
 	mem_.setEndtime(cycleCounter_, cycles);
 	mem_.updateInput();
 
+	/* romdev: once a breakpoint/watchdog hit has frozen this CPU, KEEP it
+	 * frozen — the libretro glue re-enters process() until the frame ends,
+	 * which would resume execution and drift the registers away from the hit.
+	 * The host clears the hit (pcbreak_get clearHit / pcbreak_set) to resume. */
+	/* CONSUME the requested cycle budget (advance the clock without executing)
+	 * so runFor still reports progress — an instant return makes the glue's
+	 * sample loop spin forever (the same hang the fire path's comment warns
+	 * about). */
+	if (romdev_is_frozen()) {
+		cycleCounter_ += cycles;
+		return;
+	}
+
 	unsigned char a = a_;
 	unsigned long cycleCounter = cycleCounter_;
 
@@ -515,6 +628,28 @@ void CPU::process(unsigned long const cycles) {
 				cycleCounter += cycles + (-cycles & 3);
 			}
 		} else while (cycleCounter < mem_.nextEventTime()) {
+				/* romdev (0.80.0): pc right now is the first byte of the instruction
+				 * about to dispatch — latch it for the mid-instruction R/W hooks (the
+				 * WRITE/READ watchpoints see an operand-advanced pc). Then ask the
+				 * shared lib (romdev_debug.c) whether this dispatch should FREEZE —
+				 * coverage logging + watchdog + pc-break/single-step all live there now. */
+				romdev_gb_instr_pc = pc;
+
+				if (romdev_on_dispatch(pc)) {
+					/* take the GB register snapshot (per-CPU layout) with the kind the
+					 * shared lib reports (2=watchdog, else 1=pc-break/step), then FREEZE:
+					 * save the CPU epilogue with `pc` UNCHANGED (so getCPUState reads the
+					 * break PC) but CONSUME the remaining cycle budget so the libretro frame
+					 * loop COMPLETES this frame — else process() returns with cycleCounter <
+					 * endtime and retro_run re-calls runFor forever (hang). Instruction NOT run. */
+					ROMDEV_GB_SNAP(romdev_pc_hit_kind() == 2 ? 2 : 1);
+					pc_ = pc;
+					a_ = a;
+					cycleCounter_ = mem_.nextEventTime();
+					break;                /* outer `while` sees the freeze + exits cleanly */
+				}
+
+
 			unsigned char opcode;
 
 			PC_READ(opcode);
diff --git a/libgambatte/src/cpu.h b/libgambatte/src/cpu.h
index 76c29de..7b52f43 100644
--- a/libgambatte/src/cpu.h
+++ b/libgambatte/src/cpu.h
@@ -56,6 +56,54 @@ public:
    void *rombank1_ptr() const { return mem_.rombank1_ptr(); }
    void *zeropage_ptr() const { return mem_.zeropage_ptr(); }
    void *oamram_ptr() const { return mem_.oamram_ptr(); }
+   /* rom-dev-mcp: extra state accessors for inspectSprites/Palette/etc. */
+   void *iohram_ptr() const { return mem_.iohram_ptr(); }
+   void *bgpdata_ptr() const { return mem_.bgpdata_ptr(); }
+   void *objpdata_ptr() const { return mem_.objpdata_ptr(); }
+
+   /* rom-dev-mcp: 18-byte snapshot of the SM83 CPU state.
+    * Layout (little-endian):
+    *   0-1: pc
+    *   2-3: sp
+    *   4:   a
+    *   5:   f  (synthesized: Z N H C in bits 7..4, low nybble zero)
+    *   6:   b   7:   c
+    *   8:   d   9:   e
+    *  10:   h  11:   l
+    *  12:   ime    (0/1)
+    *  13:   halt   (0/1)
+    *  14-17: reserved (zero)
+    * gambatte stores flags split across hf1/hf2/zf/cf — synthesize the
+    * standard SM83 F register from them. zf is INVERTED: zf=0 means
+    * Z bit is set. */
+   void cpu_snapshot(unsigned char *out) const {
+      unsigned f = 0;
+      if (zf == 0)              f |= 0x80;            /* Z */
+      if ((hf2 ^ hf1) & 0x10)   f |= 0x20;            /* H */
+      if (cf & 0x100)           f |= 0x10;            /* C */
+      /* N flag isn't stored separately by gambatte; default 0. */
+      out[0] = pc_ & 0xFF; out[1] = (pc_ >> 8) & 0xFF;
+      out[2] = sp & 0xFF;  out[3] = (sp >> 8) & 0xFF;
+      out[4] = a_; out[5] = (unsigned char)f;
+      out[6] = b;  out[7] = c;
+      out[8] = d;  out[9] = e;
+      out[10] = h; out[11] = l;
+      out[12] = mem_.ime() ? 1 : 0;
+      out[13] = mem_.halted() ? 1 : 0;
+      out[14] = 0; out[15] = 0; out[16] = 0; out[17] = 0;
+   }
+
+   /* rom-dev-mcp RE round 2: register read/write by romdev reg-id (the inverse
+    * of getCPUState; drives callSubroutine / setRegister). Defined in cpu.cpp so
+    * the file-local flag helpers (toF / zfFromF / hf2FromF / cfFromF) are visible.
+    * SM83 reg-id convention (STABLE across cores): 0=A 1=F 2=B 3=C 4=D 5=E 6=H
+    * 7=L 16=PC 18=SP. The setreg of PC writes pc_ (the field CPU::process loads
+    * into its `pc` local at the top of the outer loop) and SP writes the `sp`
+    * member; A writes a_ (the field process loads into its `a` local at entry) —
+    * so a setreg taken BEFORE the next process() actually sticks. Returns 0 on
+    * success, -1 on an unknown id. */
+   int      romdev_setReg(int regId, unsigned value);
+   unsigned romdev_getReg(int regId) const;
 #endif
 
 	void setVideoBuffer(video_pixel_t *videoBuf, std::ptrdiff_t pitch) {
diff --git a/libgambatte/src/gambatte-memory.cpp b/libgambatte/src/gambatte-memory.cpp
index c8790fc..5028a98 100644
--- a/libgambatte/src/gambatte-memory.cpp
+++ b/libgambatte/src/gambatte-memory.cpp
@@ -24,6 +24,8 @@
 #include "bootloader.h"
 #include <cstring>
 
+extern "C" int romdev_irq_block;
+
 namespace gambatte {
 
 Memory::Memory(Interrupter const &interrupter) :
@@ -358,7 +360,14 @@ unsigned long Memory::event(unsigned long cc) {
 			intreq_.setEventTime<intevent_unhalt>(disabled_time);
 		}
 
-		if (ime()) {
+		/* romdev INTERRUPT BLOCK — the pure-call primitive. While set, IRQ
+		 * SERVICE is suppressed (pending flags stay pending in IF), so a
+		 * routine driven by callSubroutine cannot be hijacked by the game's
+		 * own VBlank/timer handlers — the frame logic that stomps output
+		 * buffers. Video/timers keep running; only the dispatch is gated.
+		 * (Declared extern "C" at file scope above — block-scope linkage
+		 * specs are illegal and namespace scope would mangle.) */
+		if (ime() && !romdev_irq_block) {
 			unsigned const pendingIrqs = intreq_.pendingIrqs();
 			unsigned const n = pendingIrqs & -pendingIrqs;
 			unsigned address;
diff --git a/libgambatte/src/gambatte-memory.h b/libgambatte/src/gambatte-memory.h
index d82ca85..19b71d7 100644
--- a/libgambatte/src/gambatte-memory.h
+++ b/libgambatte/src/gambatte-memory.h
@@ -39,6 +39,19 @@ public:
    
 	explicit Memory(Interrupter const &interrupter);
 	bool loaded() const { return cart_.loaded(); }
+	/* romdev watchpoint: absolute ROM-file offset of a CPU address using the
+	 * CURRENTLY-mapped bank (rmem(area) is host_ptr - area_base, like NES Page[]),
+	 * so a $4000-$7FFF PC resolves to the live switchable bank. Returns
+	 * 0xFFFFFFFF when the address isn't in mapped ROM. */
+	unsigned long romdev_romOffset(unsigned pc) const {
+		const unsigned char *rm = cart_.rmem(pc >> 12);
+		const unsigned char *base = cart_.romdata(0);
+		if (!rm || !base) return 0xFFFFFFFFul;
+		const unsigned char *p = rm + pc;
+		long off = (long)(p - base);
+		if (off < 0) return 0xFFFFFFFFul;
+		return (unsigned long)off;
+	}
 	void setStatePtrs(SaveState &state);
 	unsigned long saveState(SaveState &state, unsigned long cc);
 	void loadState(SaveState const &state);
@@ -62,6 +75,12 @@ public:
    void *rombank1_ptr() const { return cart_.romdata(0) + 0x4000; }
    void *zeropage_ptr() const { return (void*)(ioamhram_ + 0x0180); }
    void *oamram_ptr() const { return (void*)ioamhram_; }
+   /* rom-dev-mcp: I/O regs ($FF00-$FF7F) live at ioamhram_+0x100, 128B. */
+   void *iohram_ptr() const { return (void*)(ioamhram_ + 0x0100); }
+   /* rom-dev-mcp: GBC palette RAM accessors (delegate to LCD).
+    * Each table = 64 bytes (8 palettes x 4 colors x 2 bytes BGR555). */
+   void *bgpdata_ptr() const { return lcd_.bgpdata_ptr(); }
+   void *objpdata_ptr() const { return lcd_.objpdata_ptr(); }
 #else
    void loadSavedata() { cart_.loadSavedata(); }
    void saveSavedata() { cart_.saveSavedata(); }
diff --git a/libgambatte/src/gambatte.cpp b/libgambatte/src/gambatte.cpp
index 5e1f952..4db71c1 100644
--- a/libgambatte/src/gambatte.cpp
+++ b/libgambatte/src/gambatte.cpp
@@ -237,6 +237,35 @@ void *GB::zeropage_ptr() const {
 void *GB::oamram_ptr() const {
  return p_->cpu.oamram_ptr();
 }
+
+/* rom-dev-mcp extensions. */
+void *GB::iohram_ptr() const {
+ return p_->cpu.iohram_ptr();
+}
+
+void *GB::bgpdata_ptr() const {
+ return p_->cpu.bgpdata_ptr();
+}
+
+void *GB::objpdata_ptr() const {
+ return p_->cpu.objpdata_ptr();
+}
+
+void GB::cpu_snapshot(unsigned char *out) const {
+ p_->cpu.cpu_snapshot(out);
+}
+
+bool GB::isCgbMode() const {
+ return p_->cpu.isCgb();
+}
+
+int GB::romdev_setReg(int regId, unsigned value) {
+ return p_->cpu.romdev_setReg(regId, value);
+}
+
+unsigned GB::romdev_getReg(int regId) const {
+ return p_->cpu.romdev_getReg(regId);
+}
 #endif
 
 }
diff --git a/libgambatte/src/video.h b/libgambatte/src/video.h
index 7516ba1..154f649 100644
--- a/libgambatte/src/video.h
+++ b/libgambatte/src/video.h
@@ -158,6 +158,11 @@ class LCD
       void setColorCorrectionBrightness(float colorCorrectionBrightness);
       void setDarkFilterLevel(unsigned darkFilterLevel);
       video_pixel_t gbcToRgb32(const unsigned bgr15);
+#ifdef __LIBRETRO__
+      /* rom-dev-mcp: expose raw 64-byte GBC palette RAM tables. */
+      void *bgpdata_ptr() const { return (void*)bgpData_; }
+      void *objpdata_ptr() const { return (void*)objpData_; }
+#endif
    private:
       enum Event { MEM_EVENT, LY_COUNT }; enum { NUM_EVENTS = LY_COUNT + 1 };
       enum MemEvent { ONESHOT_LCDSTATIRQ, ONESHOT_UPDATEWY2, MODE1_IRQ, LYC_IRQ, SPRITE_MAP,
