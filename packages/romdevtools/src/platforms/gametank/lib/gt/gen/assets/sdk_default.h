// romdev single-bank GameTank: no bundled asset pack (the SDK logo/jingle needs
// the Node asset pipeline). Empty stub — the bare init skips the logo splash.
#ifndef GEN_SDK_DEFAULT_H
#define GEN_SDK_DEFAULT_H
#endif
